Now let's try to start it again in the background (note -d for detach):

`docker run -p 80:80 -d nginx:alpine`{{execute}}

Let's test again!!

Click https://[[HOST_SUBDOMAIN]]-80-[[KATACODA_HOST]].environments.katacoda.com
